# api.youyisida.com — 反代到本机 Node API（:3000）
# 备案：子域名 api 须已在阿里云 ICP 接入，否则公网 HTTP 会被拦截
# HTTPS：配置好 80 后执行 certbot --nginx -d api.youyisida.com

server {
    listen 80;
    server_name api.youyisida.com;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# certbot 会自动追加 443 ssl 块；手动参考：
# server {
#     listen 443 ssl http2;
#     server_name api.youyisida.com;
#     ssl_certificate /etc/letsencrypt/live/api.youyisida.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/api.youyisida.com/privkey.pem;
#     location / { proxy_pass http://127.0.0.1:3000; ...同上... }
# }
